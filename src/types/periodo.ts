export type Periodo = {
    id: number;
    inicio: string;
    fin: string;
    nombre: string;
}

export type PeriodoDB = {
    per_id: number;
    per_inicio: string;
    per_fin: string;
    per_nombre: string;
}